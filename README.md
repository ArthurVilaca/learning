# Arthur Fonseca Vilaca

# Introduction

change database connection on `config/database.yml`

run:
  ```
    rake db:create
    rake db:migrate
  ```

# development

- added rubocop for linter


# tests

run:
  ```
    RAILS_ENV=test && bundle exec rspec
  ```
